# Both values are optional. When unset, the current user is resolved from the
# token the Twitch CLI is authenticated with (`twitch token`). Setting
# TWITCH_USER_ID just skips that lookup.
TWITCH_USER_ID=your_numeric_user_id
TWITCH_USER_LOGIN=your_username
